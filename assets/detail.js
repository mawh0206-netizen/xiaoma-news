const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const foreign=new Set(['Reuters','BBC','Financial Times','The Guardian','TechCrunch','The Real Deal','PR Newswire']);const storyIndex=Number(new URLSearchParams(location.search).get('story'));document.documentElement.dataset.theme=localStorage.getItem('xm-theme')||'';$('#themeButton').onclick=()=>{const n=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=n;localStorage.setItem('xm-theme',n)};
function render(data){const s=data.stories[storyIndex];if(!s){$('#detailArticle').innerHTML='<h1>没有找到这条新闻</h1><a href="index.html">返回首页</a>';return}document.title=`${s.title} · 小马看世界`;const isForeign=foreign.has(s.source);const related=data.stories.map((x,i)=>({...x,i})).filter(x=>x.category===s.category&&x.i!==storyIndex).slice(0,4);const domestic=data.stories.map((x,i)=>({...x,i})).filter(x=>!foreign.has(x.source)&&x.category===s.category&&x.i!==storyIndex).slice(0,3);const body=s.detailBody||s.summary;const facts=s.keyFacts?.length?s.keyFacts:[s.title,`来源：${s.source}`,`领域：${s.category}`];$('#detailArticle').innerHTML=`<div class="detail-kicker"><span>${esc(s.category)}</span>${isForeign?'<span class="foreign-badge">国外媒体优先</span>':''}<span>${esc(s.source)} · ${esc(s.publishedLabel)}</span></div><h1>${esc(s.title)}</h1><p class="detail-deck">${esc(s.summary)}</p><div class="detail-content"><div><h2>核心内容</h2>${String(body).split('\n').filter(Boolean).map(x=>`<p>${esc(x)}</p>`).join('')}<ul class="fact-list">${facts.map(x=>`<li>${esc(x)}</li>`).join('')}</ul><div class="decision-box"><h2>小马解读</h2><p>${esc(s.whyItMatters)}</p></div><h2>阅读建议</h2><p>${isForeign?'这条新闻来自国外媒体。即使原站暂时无法访问，上述中文摘要和决策解读仍可直接阅读；原文入口保留用于条件允许时核对。':'这条新闻来自国内可访问来源，可通过右侧入口查看完整报道。'}</p>${domestic.length?`<div class="related-list"><h2>同领域国内参考</h2>${domestic.map(x=>`<a href="detail.html?story=${x.i}">${esc(x.title)} <small>· ${esc(x.source)}</small></a>`).join('')}</div>`:''}</div><aside class="source-panel"><b>信息来源</b><p>${esc(s.source)}</p><a class="primary-source" href="${esc(s.url)}" target="_blank" rel="noopener">尝试打开原文 ↗</a><a class="secondary-source" href="index.html">返回今日晨报</a>${isForeign?'<p class="access-note">原文可能受网络条件影响。本站不会镜像转载全文。</p>':''}</aside></div>${related.length?`<div class="related-list"><h2>继续阅读 · ${esc(s.category)}</h2>${related.map(x=>`<a href="detail.html?story=${x.i}">${esc(x.title)} <small>· ${esc(x.source)}</small></a>`).join('')}</div>`:''}`}
fetch('data/news.json',{cache:'no-store'}).then(r=>r.json()).then(render).catch(()=>{$('#detailArticle').innerHTML='<h1>详情加载失败</h1><p>请返回首页稍后重试。</p>'});
